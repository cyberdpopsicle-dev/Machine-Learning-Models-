# Physics-Informed Reinforcement Learning (PIRL) for Dynamical Systems Control

An implementation of a Physics-Informed Reinforcement Learning pipeline designed to optimize control inputs for non-equilibrium physical systems. This notebook couples a high-fidelity structural state space with a continuous Proximal Policy Optimization (PPO) agent to solve constrained boundary-value optimization problems directly inside Google Colab.

---

## 1. Algorithmic Framework

The system models a forced, underdamped harmonic oscillator governed by the second-order ordinary differential equation (ODE):

$$m \frac{d^2u}{dt^2} + \mu \frac{du}{dt} + ku = F(t)$$

Where:
* $m, \mu, k$ represent the mass, damping coefficient, and spring constant of the physical environment.
* $F(t) \in [-10.0, 10.0]$ is the continuous structural force generated by the trained agent.

### Optimization Objective
The agent optimizes a quadratic cost penalty mapping state deviations and instantaneous energy expenditures:

$$R_t = -\int \left( \alpha u(t)^2 + \beta \dot{u}(t)^2 + \gamma F(t)^2 \right) dt$$

---

## 2. Notebook Structure

The code is divided into three functional executable blocks:
* **Cell 1: Dependencies** — Silent installation of `gymnasium`, `stable-baselines3`, and `torch`.
* **Cell 2: Core Core Engine & Training** — Definition of the custom physics environment and execution of the 40,000-timestep PPO training loop.
* **Cell 3: Performance Visualization** — Colab-native generation of performance line graphs mapping position convergence and control force profiles.

---

## 3. Getting Started inside Google Colab

### Execution Sequence
1. Open this notebook in Google Colab.
2. Ensure your runtime type is set to **CPU** or **T4 GPU** (via *Runtime > Change runtime type*).
3. Run the cells sequentially (**Ctrl + Enter** or **Shift + Enter**).

### Real-Time Metric Tracking
To monitor policy updates and training efficiency curves through TensorBoard inside the notebook, add a code cell with the following lines before running the training script:

```python
%load_ext tensorboard
%tensorboard --logdir=./ppo_control_logs/
```

## 4.Results
* Key Methodology Attributes:
1.Action-Critic Co-design: The continuous reward structure penalizes mechanical divergence alongside high instantaneous control forces to prevent virtual actuator fatigue.

2 Semi-Implicit Integration: Implements robust energy-preserving numerical steps directly within the environment step function to maintain exact phase-space alignment.
* Graphical Representation
<img width="1389" height="490" alt="image" src="https://github.com/user-attachments/assets/34225493-da3a-4efc-81e8-c6fb459fcd6f" />
